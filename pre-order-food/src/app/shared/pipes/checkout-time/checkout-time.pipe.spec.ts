import { CheckoutTimePipe } from './checkout-time.pipe';

describe('CheckoutTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckoutTimePipe();
    expect(pipe).toBeTruthy();
  });
});
